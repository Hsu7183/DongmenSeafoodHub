# 下單與統計網站

這是依 2026-08-31 最新需求調整的簡化入口。`npm run dev` 和 `npm run build` 現在預設啟動／建立本版；原本完整 Next.js／PostgreSQL 管理系統保留，改用 `dev:full`、`build:full`、`start:full`。

## 使用流程

版面以年長攤販手機操作為主：手機單欄大圖，商品名稱 24–26px、規格 20px、主要加減按鈕 56px；隱去英文裝飾標題、SKU 與冗長說明。手機統計使用「商品／規格＋數量」大字列表，完整表格保留供電腦及列印使用。全程不顯示價格，也沒有通往昊鼎官網的商品連結。

- `/`：只有「下單」與「統計」兩個主要入口。
- `/order`：查看商品圖片、搜尋生鮮／冷凍示範商品，填數量及攤位名稱，確認後送單。
- `/receipt/:id`：訂單商品名稱、規格、數量，下載真實 PDF 或 A4 列印。僅原下單瀏覽器可讀取。
- `/stats`：依日期篩選商品總表，不同規格與單位分開統計；每 30 秒刷新，支援列印。

## 儲存與安全

上線版本使用 Sites 配置的 D1 持久資料庫，並非瀏覽器儲存。`portal_products`、`portal_orders`、`portal_order_items` 和原完整版 PostgreSQL 資料分開，不會自動混入舊示範訂單。初次上線為空訂單；商品為 8 項明確標示的自建示範資料。

訂單採伺服器端商品名稱／規格快照、原子批次寫入、唯一重試鍵，避免重複點擊重複入單。數量須為 1–9999 整數，每單最多 100 項。跨站送單、其他瀏覽器讀取訂單、無效日期、未知商品及過長內容會被拒絕。

總表是公開的商品數量彙整，不回傳攤位、備註、訂單明細、客戶資料、價格或佣金。PDF 不含價格與成本。PDF 字型在本站提供，依 SIL OFL 開源授權，可正常顯示繁體中文。

## 昊鼎商品與正式交易

依使用者最新指示，已移除 8 張 AI 商品圖，改用 7 張核對過的昊鼎官網商品原圖；完整保留品牌、文字及浮水印，沒有裁切或修圖。圖片只在本站顯示，不可點往供應商官網；來源網址僅保留在維護文件，不打包進顧客端圖片資料。整尾透抽未找到相同品項，顯示「圖片待補」，不以小卷或透抽切圈替代。圖片原檔位於 `public/products/haoding-*`，來源、核對日期、校驗值及缺圖紀錄見 `docs/product-image-sources.json`。

本次只更換圖片，沒有匯入完整商品目錄、變更示範規格／價格或啟用真實採購；示範重量、包裝及單位不代表供應商實際規格。官網圖片可能本身包含設計合成，不宣稱為未經處理的實拍。公開來源可核對不等同已獨立驗證重製授權，正式授權紀錄仍需管理者確認。

目前**不是昊鼎完整商品目錄，也不接受／轉送真實採購**。原始 `SUPPLIER_CONTENT_AUTHORIZED=false` 規則不變。原完整系統之法律與食品業者資料發布檢查未被關閉。

正式匯入仍需使用者／供應商確認完整清單與資料使用授權。最少欄位：商品編號、規格編號、名稱、規格、單位、分类、保存方式、可訂狀態、來源網址、授權依據。不得使用官網零售價當作供應商成本。

本版預設不配置 `PORTAL_ACCEPT_LIVE_ORDERS`；即使只將資料授權開啟，也不會自動接受正式訂單。啟用真實交易前，仍須完成營業資訊、隱私／交易條款、授權商品匯入及正式客戶權限／濫用防護評估。

## 維護

依使用者指示，每次完成可用並已確認的修改後直接同步 GitHub，影響網站的更新也發布到既有線上網站，不再重複詢問同步許可。GitHub Pages 入口網址維持不變；環境密鑰與未授權資料不在同步範圍。

- `npm run dev`：本機網址由開發服務輸出，預設 127.0.0.1:5173。
- `npm run build`：Cloudflare 相容前端與服務端、部署邏輯綁定及 Drizzle migrations。
- `npm run typecheck`、`npm run lint`、`npm test`：靜態檢查及原價格／權限單元測試。
- `npm run test:portal`：只對本機 localhost 進行跨瀏覽器送單、重試、統計、保護邊界與 PDF 檢查。測試資料不隨網站部署。

資料庫配置在 `.openai/hosting.json`，實際遠端資料庫由 Sites 管理。勿上傳 `.env`、`.runtime`、`.wrangler`、供應商報價表或未授權素材。
