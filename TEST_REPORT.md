# 公開 Portal 第二階段驗證 — 2026-08-31

本報告最上方適用於目前 React／Vite／Worker／D1 公開版；文末保留原簡化版及 Next.js／PostgreSQL 版的歷史結果，不能混算為本次測試。

## 已完成與測試結果

| 檢查 | 結果 |
|---|---|
| TypeScript | npm run typecheck 通過 |
| ESLint | npm run lint 通過，無警告 |
| Production build | npm run build 成功，Vite 靜態資源與 Worker |
| Portal 整合測試 | npm run test:portal：171 項通過，實際 HTTP、Worker、本機 D1 |
| 原有單元測試 | npm test：12 項通過，不替代新版流程驗收 |
| PDF | 4 份：一般訂單、70 商品七頁、短配、取消；中文文字與渲染檢查 |
| 資料遷移 | 原三張表及舊匿名訂單遷移後保留；新增欄位、表、索引與 trigger |
| 部署前資料界線 | 本機 TEST 資料未上傳；未建立正式測試訂單、未啟用真實採購 |

### 顧客身分、訂單及安全

- A 攤兩組獨立 Cookie 可讀同一戶歷史；B 攤無法讀取、修改、取消、重訂或確認 A 攤訂單。
- 舊分頁 A 登入狀態與目前 Cookie 的 B 攤不一致時，拒絕送單，不能只靠前端攤名決定歸屬。
- 每戶隨機 salt、pepper 與 PBKDF2 雜湊；資料庫 session 僅存 token 雜湊。重設 PIN、停用與登出撤銷登入。
- 並行錯誤 PIN 以原子限流限制；不同來源不能繞過每戶限制。拒絕跨站寫入、未知欄位、非法數量及日期。
- 重複及並行送單只保留一張，跨裝置相同操作鍵仍安全。
- 10 改 6，統計只算 6；原始數量 10 與前後修訂紀錄保留。取消排除有效需求，並行修改及截單不互相覆蓋。
- 常買只屬本人，排除取消、下架項目，最多 20 項；無歷史空結果由前台顯示全部商品。
- 重訂遇到下架或 SKU／名稱／規格／單位變更，列入需重選清單，不自動替代。
- 舊匿名收據保留原 Cookie 唯讀方式，不依同名攤位自動歸戶，不可進入新採購批次。

### 集中採購及配貨

- A 10＋B 20＋C 30＝需求 60；同操作鍵重試回原批次，換操作鍵或並行操作不能重複採購。
- 可供 50、配貨 51 被拒絕且舊資料不變；各攤配貨不能超過自身需求。資料庫 trigger 另阻擋超供應量寫入。
- 實配 10／20／20 時，C 原訂仍保留 30，顯示待確認；另一攤不能代為確認。
- 舊配貨版本不能確認。重新配貨會產生新版並重新要求確認；未確認不得完成，完成後不能重配。
- 支援零供應。實際建立 50 張 × 8 商品的大批次，確認請求超過原 25 KB 仍正確驗證；一般客戶請求上限未放寬。
- 訂單修改／截單、批次建立及重新配貨均測試並行衝突與整筆回滾。

### PDF 及程式檢查

一般訂單、長單換頁、短配及取消樣本均成功產生。檢查繁中、訂購量、配貨量、取消提示及頁碼；短配 PDF 同時保留原訂 30 與可配 20，不默改訂購量。另修正中英混排時字型替代造成數字複製成錯字，使用 PDFium 抽取文字複驗。

非阻斷提示：建置的延後載入 PDF 模組仍超過 500 KB；本機 Node SQLite 與 Vitest 設定有實驗／未來相容性提示。這些不是測試失敗，下載效能與後續套件升級仍需持續檢查。

供應商與配貨列印頁分開，僅讀取已儲存的確認量；從程式與欄位檢查確認不包含售價、成本、毛利或佣金。未存顧客修改時停用 PDF／列印；未存管理配貨時停用列印／完成，刷新提示捨棄草稿。

測試輸出位於忽略提交的 .runtime/portal-tests：results.json 為結果摘要，另有 PDF 及渲染樣本。測試腳本僅允許 localhost；資料為明確 TEST 名稱並在結束停用測試帳戶及臨時商品，不刪歷史。

## 尚未完成及驗證限制

- 本輪沒有自動瀏覽器端到端測試，HTTP 測試不等同於完整 UI 驗收。
- 手機／平板實機、年長攤販試用、iOS 分享下載與管理列印頁的瀏覽器列印仍待複驗。
- 渲染核對的是產出的訂單 PDF，不是管理頁的瀏覽器列印結果。
- 未進行正式站容量／濫用壓力測試、備份還原演練或真實供應商履約。
- 正式客戶帳戶須由管理者建置；舊匿名訂單人工歸戶、採購批次取消、短配拒絕與協商流程尚未提供。

## 正式營運前仍待確認

目前仍為 8 項示範商品、7 張官網原圖，缺少完整供應商目錄、正式規格及授權確認；真實採購開關未啟用。還需確認交易主體、食品／營業資料、價格與退換貨、冷鏈責任、隱私政策、資料備份、管理密碼保管，以及 4 位 PIN 與公開商品總量的營運風險。

正式設定使用獨立管理 secret 及 PIN pepper，不與本機共用，也不在 GitHub 或此報告公開。上線檢查只讀網站、公開目錄及授權邊界，不向正式資料庫寫入測試訂單。

---

# 第一階段簡化版：歷史驗證紀錄（2026-08-31）

當時 `portal/` 下單／PDF／商品總表版本：TypeScript、ESLint、12 項原單元測試，以及 36 項實際 HTTP 檢查通過。涵蓋跨瀏覽器訂單、重複／並行送出、自己的訂單限制、無效數量與日期、100 商品參數邊界、日期篩選與不同單位分開統計。

一頁訂單與 70 商品七頁 PDF 已用 PDFium 實際渲染檢視，繁中、表格、頁碼與換頁正常。Noto TrueType 完整嵌入，避開 CJK 子集映射錯誤。未對新版進行自動瀏覽器點擊或手機實機 QA；本次以 HTTP、程式檢查和 PDF 渲染驗證。

目前仍為 8 項自建示範商品，並非完整昊鼎目錄，不接受真實採購。下列保留原完整 Next.js／PostgreSQL 版本的歷史報告。

# 原 Next.js／PostgreSQL 完整版：歷史驗證紀錄

本機 Next.js production server：`http://127.0.0.1:3000`。
真實 PostgreSQL 18.4：loopback `127.0.0.1:54329`，資料保存在 `.runtime/postgres`。

| 檢查 | 實際結果 |
| --- | --- |
| Dependencies | 已安裝，package-lock.json 已保存 |
| Prisma schema | 24 個模型；已 generate 與 typecheck |
| Migrations | 2 次 migration 已實際套用，保留資料 |
| Demo seed | 10 客戶、2 供應商、8 商品、18 規格、3 價格等級、36 歷史訂單；另保留有 TEST 標記的整合測試歷史 |
| ESLint | 通過，0 errors、0 warnings |
| TypeScript | 通過 |
| Vitest | 12 / 12 通過 |
| Production build | Next.js 16.3.3 build 成功；動態頁面/API/PDF 路由均完成 |
| Production 整合測試 | 110 / 110 通過；使用實際 PostgreSQL、HTTP 與登入 cookie |
| 套件安全掃描 | npm audit：0 vulnerabilities（當次掃描結果） |
| PDF | 客戶、內部、供應商、報價 4 種 PDF 成功下載，A4 與繁體中文渲染檢視通過 |
| 正式發布防護 | launch:check 正確拒絕 Demo、未填法律資料、未確認網域與供應商授權；預期的拒絕，不代表已合規 |

## 整合測試內容

透過管理員 API 建立 TEST 供應商、匯入 TEST 商品、建立兩家獨立 TEST 客戶及專屬價格，再以客戶登入建立訂單。驗證客戶價格優先權、信用／MOQ、未授權正式匯入拒絕、重複提交、其他客戶 ID 不可越權、過期與不正確請求、歷史價格不受改價影響。

以管理員彙總兩張訂單，得到 50 件、供應商成本 NT$5,000、客戶成交額 NT$7,800、毛利 NT$2,800。供應商登入只能取得自己的採購與配貨資料，更新履約後兩張訂單到 COMPLETED；測試部分收款、全額收款、超額收款拒絕、佣金確認及已收狀態，並核對 Dashboard 與 old/new audit。

新增驗證涵蓋服務費與客戶地址的安全結帳 API、過時確認金額拒絕且不產生訂單、供應商金額議價目標、下架 TEST 商品後歷史快照仍保留。金額目標不會自動觸發數量折扣。

可機讀的逐項結果：`.runtime/verification/integration-report.json`。PDF、抽取文字及渲染樣本同存於 `.runtime/verification`。測試腳本只允許 Demo localhost，並保留明確 TEST 記錄供稽核；已完成的測試商品可停用而不刪除歷史。

## 瀏覽器驗證邊界

已在內嵌瀏覽器實際確認桌面商品目錄、未登入不顯示價格、客戶登入、搜尋商品、+5 數量控制與採購單品項／金額顯示。

切換 production server 時，既有瀏覽器停在 `ERR_CONNECTION_REFUSED` 的暫存錯誤頁，Browser 工具隨後因 URL 安全政策拒絕重載與導覽。沒有使用其他瀏覽器、原始 CDP 或安全政策繞過機制。

因此**尚未完成三角色完整瀏覽器串接操作，以及手機／平板視覺與實機複驗**；不能把 110 項 HTTP 整合測試等同於這些 UI 驗證。網站目前已重新啟動，使用者可自行開啟上述本機網址進行操作。

響應式排版與手機快速訂貨控制已實作；仍需實際裝置測試。未執行正式網域部署、真實供應商匯入或真實交易。
